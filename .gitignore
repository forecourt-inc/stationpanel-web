node_modules
.next
out
.env
.env.local
.env*.local
.vercel
.DS_Store
next-env.d.ts
*.tsbuildinfo

# Demo requests saved to disk when RESEND_API_KEY is not set
/data

# Working files, not site files. Kept locally only.
/STATIONPANEL-MARKETING-PROMPT.md
/reference/dashboard.png

# Raw device and app captures. Only cleaned copies go into /public.
/WEB_PHOTOS
